spring.datasource.url=jdbc:h2:mem:test_h2_db;MODE=PostgreSQL
spring.datasource.driverClassName=org.h2.Driver
hibernate.dialect=org.hibernate.dialect.H2Dialect
hibernate.hbm2ddl.auto=create
spring.jpa.show-sql=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2

#Path to folder you want to store files
upload.path=/home/vmartinkov/Study/springresttwitterable/uploads

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.profiles.active=test
hostname=localhost:8080

testing.host.origin.local=http://localhost

spring.session.jdbc.initialize-schema=always
spring.session.jdbc.table-name=SPRING_SESSION
spring.session.jdbc.schema=classpath:session_tables.sql

test.images.location = test/resources/image/